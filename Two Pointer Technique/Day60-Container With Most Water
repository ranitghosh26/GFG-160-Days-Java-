class Solution {
    public int maxWater(int arr[]) {
        // Code Here
        int l = 0;
        int r= arr.length-1;
        int res=0;
        
        while(l < r) 
        res =  Math.max(res, (r-l)*(arr[l] < arr[r] ? arr[l++] : arr[r--]));
        return res;
    }
}
